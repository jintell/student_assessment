# FEAT-PLAT-005 Engineering Walkthrough Record

## Session

- Task: `P10.10`
- Feature: `FEAT-PLAT-005`
- Date: 2026-09-23
- Time: 08:39 WAT / 07:39 UTC
- Format: repository-backed asynchronous walkthrough with local evidence review
- Branch/commit: `tasks/2-migration` / `4001ebcb8f72fcc34eb0a9099a81d031d3bb90e7`
- Status: **COMPLETED**

The walkthrough was requested and received in the current Codex task thread.
Only the human participant is counted as engineering attendance; the automated
agent is recorded separately as facilitator.

## Attendance

| Participant | Role | Attendance / confirmation |
|---|---|---|
| `jintell` | Repository requester and engineering representative | Present - initiated the walkthrough in this task thread |

- Facilitator: OpenAI Codex execution agent
- Session reference: current Codex task thread and this repository record

## Walkthrough Performed

### 1. Three-release phase model

Reviewed `docs/migration-expand-contract.md`, covering:

- release N `EXPAND` plus a separate worker backfill;
- release N+1 `MIGRATE`, with dual writes and reads from the new shape;
- release N+2 `CONTRACT` as a separate, forward-only release; and
- why N-1 compatibility makes code rollback safe while schema rollback is
  prohibited.

The planning exercise replaced `delivery.answer.response` with `response_json`:

| Release | Planned change | Exit evidence |
|---|---|---|
| N `EXPAND` | Add nullable `response_json`; start a resumable worker backfill | Stage 12, N-1 compatibility, migration report, observable backfill |
| N+1 `MIGRATE` | Dual-write both columns and read `response_json` | Backfill complete, dual-write observed, rollback image compatible |
| N+2 `CONTRACT` | Drop `response` in a separate forward-only release | Observation window complete and old readers/writers retired |

### 2. Header contract

Inspected the header contract in `docs/migration-authoring.md`:

```sql
-- cbt:phase EXPAND
-- cbt:module delivery
-- cbt:transactional false
-- cbt:justification FEAT-DLV-002 add the answer lookup index
```

Confirmed that order, exact values, module/path agreement, and transaction
mode are validated before SQL analysis. The own-schema rule and
one-classification-per-release manifest were also reviewed.

### 3. Why CONCURRENTLY is mandatory

Compared a regular index build with `CREATE INDEX CONCURRENTLY` using
`docs/migration-forbidden-operations.md`, then reviewed the stage-12 evidence
that a blocking `ALTER TABLE` fails while the concurrent build is classified
`ONLINE_COMPATIBLE`.

Run or review:

```bash
./gradlew :migration-verify:test \
  --tests '*LockMeasurementHarnessIntegrationTest*'
```

Observed on 2026-09-23: **PASS**. The tests exercise blocking lock detection,
permitted concurrent index construction, and lock-timeout behavior.

### 4. INVALID index hazard

Reviewed `docs/runbook-migration-failure.md`, covering:

- how interruption of a non-transactional concurrent build can leave
  `pg_index.indisvalid = false`;
- why retry first uses `DROP INDEX CONCURRENTLY`;
- how Flyway history is repaired only after schema reconciliation; and
- why the idempotent operator command is safe when no invalid index exists.

Run or review:

```bash
./gradlew :migration-verify:test \
  --tests '*InvalidIndexReconcilerTest*'
```

Observed on 2026-09-23: **PASS**. The reconciler is idempotent and removes only
attributable invalid concurrent-index artifacts before retry.

### 5. Local stage 12 and retained evidence

Reviewed `docs/evidence/FEAT-PLAT-005/P7.20-clean-pipeline-run.md`, including
the green stage-12 run and the forced failure that prevented stage 13. The
retained lock-duration, N-1 compatibility, rollback, and backfill evidence was
connected to `docs/evidence/verification-evidence-register.md`.

## Questions and Outcomes

| Topic | Question or decision | Owner | Due date |
|---|---|---|---|
| Required walkthrough scope | Covered with no unresolved follow-up | N/A | N/A |

## Completion Confirmation

The session covered the phase model, exact four-line header contract,
concurrent-index rule, invalid-index recovery, and the three-release planning
exercise. Attendance and executable evidence are retained above.

- Recorded by: OpenAI Codex execution agent
- Recorded on: 2026-09-23

Task disposition: **P10.10 COMPLETE**
